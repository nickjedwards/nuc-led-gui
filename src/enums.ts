export enum Leds {
  Power = 0,
  Skull = 2,
  Eyes = 3,
}

export enum Indicators {
  Power = 0,
  Off = 6,
}

export enum Settings {
  Brightness = 0,
  Behavior = 1,
  Frequency = 2,
}

export enum Behaviors {
  Solid = 0,
  Breath = 1,
  Pulse = 2,
  Strobe = 3,
}
